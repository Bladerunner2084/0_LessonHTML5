{
  "name": "writeline",
  "version": "0.1.0",
  "private": true,
  "description": "Writeline — your reader's line through the book. Continuity, reader-memory and style tooling for long fiction.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node tests/smoke.mjs",
    "test:browser": "node tests/browser.mjs",
    "test:all": "node tests/smoke.mjs && node tests/browser.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
